local OrionLib = loadstring(game:HttpGet(('https://raw.githubusercontent.com/shlexware/Orion/main/source')))()
local Window = OrionLib:MakeWindow({Name = "Poor Artist Hub by LR", HidePremium = false, SaveConfig = true, ConfigFolder = "OrionTest"}) 
local Tab = Window:MakeTab({
	Name = "Generate art",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
})
local Section = Tab:AddSection({
	Name = "Arts"
})
Tab:AddButton({
	Name = "Anime Girl 1",
	Callback = function()	loadstring(game:HttpGet('https://dpaste.com/6X4RRGT5M.txt'))()
      local textLabel = game.Players.LocalPlayer.PlayerGui.MainGui.PaintFrame.NextButton.Label
        local mins = 5
        local secs = 0
        
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "Timer",
            Text = "The timer has started.",
            Duration = 5
        })
    
        repeat
            if string.len(secs) < 2 then
                textLabel.Text = (mins .. ":0" .. secs)  
            else 
                textLabel.Text = (mins .. ":" .. secs)  
            end  
            
            if secs == 0 then
                secs = 60
                mins = mins - 1
            end
            secs = secs - 1
            wait(1)
        until mins == 0 and secs == 0
        
        textLabel.Text = "Ready!!"
        
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "Timer",
            Text = "The timer has ended.",
            Duration = 5
        })
      
  	end    
})
Tab:AddButton({
	Name = "Anime Girl 2",
	Callback = function()
      		loadstring(game:HttpGet('https://dpaste.com/7XTCGL8WS.txt'))()
      local textLabel = game.Players.LocalPlayer.PlayerGui.MainGui.PaintFrame.NextButton.Label
        local mins = 5
        local secs = 0
        
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "Timer",
            Text = "The timer has started.",
            Duration = 5
        })
    
        repeat
            if string.len(secs) < 2 then
                textLabel.Text = (mins .. ":0" .. secs)  
            else 
                textLabel.Text = (mins .. ":" .. secs)  
            end  
            
            if secs == 0 then
                secs = 60
                mins = mins - 1
            end
            secs = secs - 1
            wait(1)
        until mins == 0 and secs == 0
        
        textLabel.Text = "Ready!!"
        
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "Timer",
            Text = "The timer has ended.",
            Duration = 5
        })
      
  	end    
})
Tab:AddButton({
	Name = "Anime Girl 3",
	Callback = function()		loadstring(game:HttpGet('https://dpaste.com/2QENJHQQ5.txt'))()
      local textLabel = game.Players.LocalPlayer.PlayerGui.MainGui.PaintFrame.NextButton.Label
        local mins = 5
        local secs = 0
        
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "Timer",
            Text = "The timer has started.",
            Duration = 5
        })
    
        repeat
            if string.len(secs) < 2 then
                textLabel.Text = (mins .. ":0" .. secs)  
            else 
                textLabel.Text = (mins .. ":" .. secs)  
            end  
            
            if secs == 0 then
                secs = 60
                mins = mins - 1
            end
            secs = secs - 1
            wait(1)
        until mins == 0 and secs == 0
        
        textLabel.Text = "Ready!!"
        
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "Timer",
            Text = "The timer has ended.",
            Duration = 5
        })
      
  	end    
})
Tab:AddButton({
	Name = "Anime Girl 4 ",
	Callback = function()
      		loadstring(game:HttpGet('https://dpaste.com/7ZA6RK2HK.txt'))()
      local textLabel = game.Players.LocalPlayer.PlayerGui.MainGui.PaintFrame.NextButton.Label
        local mins = 5
        local secs = 0
        
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "Timer",
            Text = "The timer has started.",
            Duration = 5
        })
    
        repeat
            if string.len(secs) < 2 then
                textLabel.Text = (mins .. ":0" .. secs)  
            else 
                textLabel.Text = (mins .. ":" .. secs)  
            end  
            
            if secs == 0 then
                secs = 60
                mins = mins - 1
            end
            secs = secs - 1
            wait(1)
        until mins == 0 and secs == 0
        
        textLabel.Text = "Ready!!"
        
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "Timer",
            Text = "The timer has ended.",
            Duration = 5
        })
      
  	end    
})
Tab:AddButton({
	Name = "Anime Girl 5",
	Callback = function()
      loadstring(game:HttpGet('https://dpaste.com/3MJ934J34.txt'))()
      local textLabel = game.Players.LocalPlayer.PlayerGui.MainGui.PaintFrame.NextButton.Label
        local mins = 5
        local secs = 0
        
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "Timer",
            Text = "The timer has started.",
            Duration = 5
        })
    
        repeat
            if string.len(secs) < 2 then
                textLabel.Text = (mins .. ":0" .. secs)  
            else 
                textLabel.Text = (mins .. ":" .. secs)  
            end  
            
            if secs == 0 then
                secs = 60
                mins = mins - 1
            end
            secs = secs - 1
            wait(1)
        until mins == 0 and secs == 0
        
        textLabel.Text = "Ready!!"
        
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "Timer",
            Text = "The timer has ended.",
            Duration = 5
        })
      
  	end    
})



Tab:AddButton({
	Name = "Anime Boy",
	Callback = function()
      		loadstring(game:HttpGet('https://dpaste.com/G3ZB6NKKY.txt'))() 
      local textLabel = game.Players.LocalPlayer.PlayerGui.MainGui.PaintFrame.NextButton.Label
        local mins = 5
        local secs = 0
        
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "Timer",
            Text = "The timer has started.",
            Duration = 5
        })
    
        repeat
            if string.len(secs) < 2 then
                textLabel.Text = (mins .. ":0" .. secs)  
            else 
                textLabel.Text = (mins .. ":" .. secs)  
            end  
            
            if secs == 0 then
                secs = 60
                mins = mins - 1
            end
            secs = secs - 1
            wait(1)
        until mins == 0 and secs == 0
        
        textLabel.Text = "Ready!!"
        
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "Timer",
            Text = "The timer has ended.",
            Duration = 5
        })
      
  	end    
})
Tab:AddButton({
	Name = "Anime Boy 2",
	Callback = function()
      		loadstring(game:HttpGet('https://dpaste.com/FFCCQTULL.txt'))()
      local textLabel = game.Players.LocalPlayer.PlayerGui.MainGui.PaintFrame.NextButton.Label
        local mins = 5
        local secs = 0
        
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "Timer",
            Text = "The timer has started.",
            Duration = 5
        })
    
        repeat
            if string.len(secs) < 2 then
                textLabel.Text = (mins .. ":0" .. secs)  
            else 
                textLabel.Text = (mins .. ":" .. secs)  
            end  
            
            if secs == 0 then
                secs = 60
                mins = mins - 1
            end
            secs = secs - 1
            wait(1)
        until mins == 0 and secs == 0
        
        textLabel.Text = "Ready!!"
        
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "Timer",
            Text = "The timer has ended.",
            Duration = 5
        })
      
  	end    
})
Tab:AddButton({
	Name = "Anime Boy 3",
	Callback = function()
	loadstring(game:HttpGet('https://dpaste.com/HZG8E5VGM.txt'))()
	local textLabel = game.Players.LocalPlayer.PlayerGui.MainGui.PaintFrame.NextButton.Label
        local mins = 5
        local secs = 0
        
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "Timer",
            Text = "The timer has started.",
            Duration = 5
        })
    
        repeat
            if string.len(secs) < 2 then
                textLabel.Text = (mins .. ":0" .. secs)  
            else 
                textLabel.Text = (mins .. ":" .. secs)  
            end  
            
            if secs == 0 then
                secs = 60
                mins = mins - 1
            end
            secs = secs - 1
            wait(1)
        until mins == 0 and secs == 0
        
        textLabel.Text = "Ready!!"
        
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "Timer",
            Text = "The timer has ended.",
            Duration = 5
        })
        
  	end    
})
Tab:AddButton({
	Name = "Anime Boy 4",
	Callback = function()
      		loadstring(game:HttpGet('https://dpaste.com/GZLELUH82.txt'))()
      local textLabel = game.Players.LocalPlayer.PlayerGui.MainGui.PaintFrame.NextButton.Label
        local mins = 5
        local secs = 0
        
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "Timer",
            Text = "The timer has started.",
            Duration = 5
        })
    
        repeat
            if string.len(secs) < 2 then
                textLabel.Text = (mins .. ":0" .. secs)  
            else 
                textLabel.Text = (mins .. ":" .. secs)  
            end  
            
            if secs == 0 then
                secs = 60
                mins = mins - 1
            end
            secs = secs - 1
            wait(1)
        until mins == 0 and secs == 0
        
        textLabel.Text = "Ready!!"
        
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "Timer",
            Text = "The timer has ended.",
            Duration = 5
        })
      
  	end    
})
local MiscTab = Window:MakeTab({
	Name = "Misc",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
})
MiscTab:AddButton({
	Name = "Redeem Codes",
	Callback = function()
      		local codes = {
   "christmas",
   "tipjar",
   "hallowart",
   "frankenpablo",
   "art300",
   "colors300",
   "brush250",
   "paint300",
   "starvingart",
   "100million",
   "paintbrush250",
   "pixelart",
   "fartist",
   "pablo250",
   "monalisa200",
   "easterart",
   "picasso250",
   "pablo300",
   "bobux",
   "artcoin100",
   "starving",
   "egghunt",
   "BOBUX",
   "clover",
}

local remotes = game:GetService("ReplicatedStorage").Remotes.Codes

local function executeCode(code)
   local args = {[1] = code}
   remotes:InvokeServer(unpack(args))
end

local function run()
   for i = 1, #codes do
       executeCode(codes[i])
   end
end

for j = 1, 21 do
   coroutine.wrap(run)()
   wait(2.75)
      end
  	end    
})
MiscTab:AddButton({
	Name = "Server hop",
	Callback = function()
loadstring(game:HttpGet('https://raw.githubusercontent.com/noobhosting/noobscript/main/AutoServerHop.lua'))()
  	end    
})
MiscTab:AddButton({
	Name = "Rejoin Server",
	Callback = function()
local ts = game:GetService("TeleportService")

local p = game:GetService("Players").LocalPlayer

 

ts:Teleport(game.PlaceId, p)
  	end    
})

local BobuxTab = Window:MakeTab({
	Name = "Free robuxs",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
})
BobuxTab:AddButton({
	Name = "Get potential buyers",
	Callback = function()
      		local Players = game:GetService("Players")
local MarketplaceService = game:GetService("MarketplaceService")
function GetAvatarWorth(id)
    local info = Players:GetCharacterAppearanceInfoAsync(id) -- Get the info
    local items = info.assets -- Get player items
    local Avatar_Worth = 0 -- Avatar worth in Robux
    for x,v in pairs(items) do -- For every key/value pair in items do...
        local ProductInfo = MarketplaceService:GetProductInfo(v.id,Enum.InfoType.Asset) -- Get product info
        if ProductInfo.IsForSale then -- If it's for sale...
            Avatar_Worth += ProductInfo.PriceInRobux -- Add price in robux
        end
    end
    return Avatar_Worth -- Return avatar worth
end
 local function RunScript()
local players = game:GetService("Players"):GetPlayers()
local player = players[math.random(1, #players)]
    print(player.Name .. "'s avatar is worth " .. GetAvatarWorth(player.UserId) .. " Robux")
   end
local reploop = true 
while true do
RunScript()
wait(10)
  local players = game:GetService("Players"):GetPlayers()
 local player = players[math.random(1, #players)]
  game:GetService("StarterGui"):SetCore("SendNotification",{
    Title = "Rich Sniper by LeRat", -- Required
    Text = player.DisplayName .. "'s avatar is worth " .. GetAvatarWorth(player.UserId) .. " Robux", -- Required
    Icon = "rbxassetid://13488634726" -- Optional
})
      end
  	end    
})
BobuxTab:AddButton({
	Name = "Rich server teleport",
	Callback = function()
      		--\\settings
local minRichPeople = 5
local minBought = 100
local delay = true
local message = "server hop"
game.StarterGui:SetCore("SendNotification", {
            Title = "LR Hub";
            Text = message ;
            Icon = "rbxassetid://13488634726";
            Duration = 15;
  })
    --server checker
    minBought=minBought-1
   
    local moneyValues={}
    local attractive=0

    for i,v in pairs(game:GetService("Players"):GetDescendants()) do
        if v.Name=="Bought" and v:IsA("IntValue") and v.Parent.Name=="leaderstats" then
            table.insert(moneyValues,v.Value)
        end
    end

    for i,v in pairs(moneyValues) do
        if v>minBought then
            attractive = attractive+ 1
        end
    end

    if attractive < minRichPeople then
        print("not attractive")
  message = "Poor server"
  game.StarterGui:SetCore("SendNotification", {
            Title = "LR Hub";
            Text = message ;
            Icon = "rbxassetid://13488634726";
            Duration = 15;
  })
  wait(5)
        if delay then 
local Player = game.Players.LocalPlayer    
local Http = game:GetService("HttpService")
local TPS = game:GetService("TeleportService")
local Api = "https://games.roblox.com/v1/games/"

local _place,_id = game.PlaceId, game.JobId
local _servers = Api.._place.."/servers/Public?sortOrder=Desc&limit=100"
function ListServers(cursor)
   local Raw = game:HttpGet(_servers .. ((cursor and "&cursor="..cursor) or ""))
   return Http:JSONDecode(Raw)
end

local Next; repeat
   local Servers = ListServers(Next)
   for i,v in next, Servers.data do
       if v.playing < v.maxPlayers and v.id ~= _id then
           local s,r = pcall(TPS.TeleportToPlaceInstance,TPS,_place,v.id,Player)
           if s then break end
       end
   end
   
   Next = Servers.nextPageCursor
until not Next
    else
        print("attractive")
message = "Rich server"
    game.StarterGui:SetCore("SendNotification", {
            Title = "LR Hub";
            Text = message ;
            Icon = "rbxassetid://13488634726";
            Duration = 15;
  })

        
    end
      end
  	end    
})
local AutoTab = Window:MakeTab({
	Name = "Automated things",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
})

local phrases = {
}
AutoTab:AddTextbox({
	Name = "Custom messages",
	Default = ".",
	TextDisappear = true,
	Callback = function(Value)
		table.insert(phrases, Value)
	end	  
})
AutoTab:AddParagraph("How to use","To use you need to first type your customs message in textbox above then click 1 time on button brlo,")
AutoTab:AddButton({
	Name = "Auto Messages",
	Callback = function()
      		while true do
   game:GetService("ReplicatedStorage").DefaultChatSystemChatEvents.SayMessageRequest:FireServer(phrases[math.random(1, #phrases)], "All")
   wait(150)
      end
  	end    
})
AutoTab:AddButton({
	Name = "Auto setup",
	Callback = function()
      		loadstring(game:HttpGet('https://pastebin.com/raw/PfNxhgA7'))()
  	end    
})
local CreditsTab = Window:MakeTab({
	Name = "Credits",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
})
CreditsTab:AddParagraph("Credits","Credits to : alan11ago the mod, xanto the dev , olivier de chez Carglass the goat, lol1 the ceo , thunder hub script very op, script is made by SponsoParNordVpn#7360.Ui library : Orion library, join discord : https://discord.gg/a9Fnev6aQQ")
setclipboard("https://discord.gg/a9Fnev6aQQ") 